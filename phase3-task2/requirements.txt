pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
pyyaml>=6.0
requests>=2.28
matplotlib>=3.7

# NOTE: production would use LightGBM/XGBoost with a lambdarank objective
# (see src/training/train_model.py docstring + docs/MODEL_CARD.md "Known
# limitations"). This environment had no network egress to install them,
# so src/training/train_model.py uses scikit-learn's GradientBoostingRegressor
# behind a single swappable build_model() function instead.
